# C++ Experiments

A C++ monorepo containing distinct experimental/playground kind of projects.

## Usage

```shell
cmake -S . -B build
cmake --build build
./build/experiments/<project>/<executable> # to run project executable
```
