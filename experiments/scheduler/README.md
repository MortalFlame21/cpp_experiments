# Scheduler

A simple program that runs for `<DURATION>` seconds and schedules a function `f` for `<INTERVAL>` seconds.

## Usage

```shell
./scheduler <DURATION> <INTERVAL>
```

- `<DURATION>` and `<INTERVAL>` are in seconds.
